'use strict'

module.exports = {
    // coa: (model, db) => {
    //    model.hasMany(db.model('coa_detail'), {foreignKey: 'coa_id'});
    //    model.belongsTo(db.model('coa_type'), {foreignKey: 'ctype_id'});
    // }
}
